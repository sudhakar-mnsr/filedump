apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.10'
}

jar {
  manifest {
    attributes(
      'Class-Path': configurations.compile,
      'Main-Class': 'helloWorkshop'
    )
  }
}
